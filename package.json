{
  "name": "now-micro",
  "version": "1.0.0",
  "main": "lib/index.js",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^4.0.0",
    "micro": "^7.0.6",
    "redis": "^2.6.5"
  },
  "scripts": {
    "start": "node --harmony-async-await node_modules/.bin/micro .",
    "build": "babel src -d lib",
    "dev": "yarn build && yarn start"
  },
  "engine": {
    "node": "7"
  },
  "devDependencies": {
    "babel-cli": "^6.23.0",
    "babel-plugin-transform-object-rest-spread": "^6.23.0",
    "babel-preset-env": "^1.1.8"
  }
}
